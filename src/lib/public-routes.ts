/**
 * Prefijos de rutas públicas que no requieren autenticación
 * y no deben renderizar el TopBar interno.
 */
export const PUBLIC_PATH_PREFIXES = ['/login', '/signup', '/registro', '/activation', '/conteo'];
